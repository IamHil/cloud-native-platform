apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-learning-data
  namespace: cloud-native
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
